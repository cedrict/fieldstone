#! /bin/bash

rm errors_0
rm errors_1
rm *.ascii

for l in 8 12 16 24 32 ; do

for e in 0 1; do

    echo res: $l X $l element $e

        python3 fieldstone.py $l $l 0 $e > opla

        grep errq opla >> errors_$e

done
done
